# TrueForge server URL (default for local mode)
VITE_TRUEFORGE_BASE_URL=http://localhost:8790

# OIDC token — only needed for hosted deployments with login enabled.
# Leave empty for local mode (npx @truefoundry/trueforge).
VITE_TRUEFORGE_TOKEN=
